# Local dev uses Vite proxy, so these stay relative.
# Production build points at deployed backends.
VITE_AUTH_URL=/api/auth
VITE_ITEM_URL=/api/items
VITE_ORDERS_URL=/api/orders
VITE_KITCHEN_URL=/api/kitchen
